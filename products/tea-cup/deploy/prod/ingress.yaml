apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tea-cup
  namespace: prod
spec:
  ingressClassName: nginx
  rules:
    - host: tea-cup-prod.labotomy.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tea-cup
                port:
                  number: 80
