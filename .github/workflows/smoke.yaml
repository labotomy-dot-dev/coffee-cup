name: Smoke test on production after ArgoCD sync

on:
  repository_dispatch:
    types: [argocd-sync-succeeded]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  debug:
    name: Print ArgoCD webhook payload
    runs-on: ubuntu-latest
    if: ${{ github.event.client_payload.project == 'prod' }}
    steps:
      - name: Print payload
        run: |
          echo "Event type: ${{ github.event.action || github.event_name }}"
          echo "Payload: ${{ toJson(github.event.client_payload) }}"
  smoke_tests:
    name: Run production smoke tests
    # ✅ Only trigger for ArgoCD apps belonging to the prod project
    if: ${{ github.event.client_payload.project == 'prod' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Print ArgoCD webhook payload
        run: |
          echo "ArgoCD Payload: ${{ github.event.client_payload }}"
          echo "✅ ArgoCD sync succeeded for production!"

      - name: Run production smoke test
        shell: bash
        run: |
          ARGO_APP="${{ github.event.client_payload.app }}"
          APP_BASE=$(echo "$ARGO_APP" | sed -E 's/-[^-]+$//')
          echo "Running smoke test for $APP_BASE..."
          # Example:
          # curl -f "https://prod.example.com/$APP_BASE/health" || exit 1
          echo "✅ Smoke test passed!"
  rollback-on-failure:
    name: Rollback on failure
    runs-on: ubuntu-latest
    needs: smoke_tests
    if: failure()
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Rollback to previous image version
        shell: bash
        run: ./github/workflows/scripts/rollback.sh
